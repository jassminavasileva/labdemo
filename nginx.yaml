apiVersion: v1
kind: Pod
metadata:
  labels:
    run: nginx-yaml
    method: fromcode
    another: label
    anotherlabel: anotherlabel
    name: nginx-yaml
spec:
  containers:
  - image: nginx
    name: nginx-yaml
